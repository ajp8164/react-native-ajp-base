import { TextStyle, ViewStyle } from 'react-native';

export type Styles = {
  swipeableListItemContainer: ViewStyle;
  textGiant: TextStyle;
  textMicro: TextStyle;
  view: ViewStyle;
  viewAlt: ViewStyle;
  viewInv: ViewStyle;
};
